{
  "name": "mouse-mapp-predict",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@anzp/azure-function-multipart": "^3.1.0",
    "@tensorflow/tfjs": "^4.2.0"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "16.x",
    "azure-functions-core-tools": "^4.x",
    "typescript": "^4.0.0"
  }
}
